{
  "name": "@mediklinik/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "swagger": "echo 'Swagger generation will be wired after NestJS module bootstrap is connected.'",
    "test": "bun test",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@mediklinik/types": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "typescript": "^5.6.3"
  }
}
