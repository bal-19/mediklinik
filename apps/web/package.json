{
  "name": "@mediklinik/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bunx vite",
    "build": "tsc -p tsconfig.json && bunx vite build",
    "lint": "tsc -p tsconfig.json --noEmit",
    "preview": "bunx vite preview",
    "test": "bun test ./tests/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@mediklinik/types": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
